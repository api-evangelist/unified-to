naftiko: 1.0.0-alpha2
info:
  label: Unified.to  API — trialbalance
  description: 'Unified.to  API — trialbalance. 2 operations. Lead operation: List All Trialbalances. Self-contained Naftiko
    capability covering one Unified To business surface.'
  tags:
  - Unified To
  - trialbalance
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    UNIFIED_TO_API_KEY: UNIFIED_TO_API_KEY
capability:
  consumes:
  - type: http
    namespace: full-trialbalance
    baseUri: https://api.unified.to
    description: Unified.to  API — trialbalance business capability. Self-contained, no shared references.
    resources:
    - name: accounting-connection_id-trialbalance
      path: /accounting/{connection_id}/trialbalance
      operations:
      - name: listaccountingtrialbalances
        method: GET
        description: List All Trialbalances
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: number
        - name: offset
          in: query
          type: number
        - name: updated_gte
          in: query
          type: string
          description: Return only results whose updated date is equal or greater to this value (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ
            format)
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: query
          in: query
          type: string
          description: Query string to search. eg. email address or name
        - name: start_gte
          in: query
          type: string
          description: The start date to filter by (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ format)
        - name: end_lt
          in: query
          type: string
          description: The end date to filter by (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ format)
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
    - name: accounting-connection_id-trialbalance-id
      path: /accounting/{connection_id}/trialbalance/{id}
      operations:
      - name: getaccountingtrialbalance
        method: GET
        description: Retrieve a Trialbalance
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: id
          in: path
          type: string
          description: ID of the Trialbalance
          required: true
    authentication:
      type: apikey
      key: authorization
      value: '{{env.UNIFIED_TO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: full-trialbalance-rest
    port: 8080
    description: REST adapter for Unified.to  API — trialbalance. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/accounting/{connection-id}/trialbalance
      name: accounting-connection-id-trialbalance
      description: REST surface for accounting-connection_id-trialbalance.
      operations:
      - method: GET
        name: listaccountingtrialbalances
        description: List All Trialbalances
        call: full-trialbalance.listaccountingtrialbalances
        with:
          limit: rest.limit
          offset: rest.offset
          updated_gte: rest.updated_gte
          sort: rest.sort
          order: rest.order
          query: rest.query
          start_gte: rest.start_gte
          end_lt: rest.end_lt
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/accounting/{connection-id}/trialbalance/{id}
      name: accounting-connection-id-trialbalance-id
      description: REST surface for accounting-connection_id-trialbalance-id.
      operations:
      - method: GET
        name: getaccountingtrialbalance
        description: Retrieve a Trialbalance
        call: full-trialbalance.getaccountingtrialbalance
        with:
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: full-trialbalance-mcp
    port: 9090
    transport: http
    description: MCP adapter for Unified.to  API — trialbalance. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-all-trialbalances
      description: List All Trialbalances
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: full-trialbalance.listaccountingtrialbalances
      with:
        limit: tools.limit
        offset: tools.offset
        updated_gte: tools.updated_gte
        sort: tools.sort
        order: tools.order
        query: tools.query
        start_gte: tools.start_gte
        end_lt: tools.end_lt
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieve-trialbalance
      description: Retrieve a Trialbalance
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: full-trialbalance.getaccountingtrialbalance
      with:
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
