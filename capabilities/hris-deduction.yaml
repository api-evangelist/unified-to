naftiko: 1.0.0-alpha2
info:
  label: Unified.to  API — deduction
  description: 'Unified.to  API — deduction. 6 operations. Lead operation: List all deductions. Self-contained Naftiko capability
    covering one Unified To business surface.'
  tags:
  - Unified To
  - deduction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    UNIFIED_TO_API_KEY: UNIFIED_TO_API_KEY
capability:
  consumes:
  - type: http
    namespace: hris-deduction
    baseUri: https://api.unified.to
    description: Unified.to  API — deduction business capability. Self-contained, no shared references.
    resources:
    - name: hris-connection_id-deduction
      path: /hris/{connection_id}/deduction
      operations:
      - name: listhrisdeductions
        method: GET
        description: List all deductions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: number
        - name: offset
          in: query
          type: number
        - name: updated_gte
          in: query
          type: string
          description: Return only results whose updated date is equal or greater to this value (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ
            format)
        - name: sort
          in: query
          type: string
        - name: order
          in: query
          type: string
        - name: query
          in: query
          type: string
          description: Query string to search. eg. email address or name
        - name: user_id
          in: query
          type: string
          description: The user/employee ID to filter by (reference to HrisEmployee)
        - name: company_id
          in: query
          type: string
          description: The company ID to filter by (reference to HrisCompany)
        - name: payslip_id
          in: query
          type: string
          description: The payslip ID to filter by
        - name: benefit_id
          in: query
          type: string
          description: The benefit ID to filter by
        - name: start_gte
          in: query
          type: string
          description: The start date to filter by (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ format)
        - name: end_lt
          in: query
          type: string
          description: The end date to filter by (ISO-8601 / YYYY-MM-DDTHH:MM:SSZ format)
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
      - name: createhrisdeduction
        method: POST
        description: Create a deduction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: hris-connection_id-deduction-id
      path: /hris/{connection_id}/deduction/{id}
      operations:
      - name: removehrisdeduction
        method: DELETE
        description: Remove a deduction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: id
          in: path
          type: string
          description: ID of the Deduction
          required: true
      - name: gethrisdeduction
        method: GET
        description: Retrieve a deduction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: id
          in: path
          type: string
          description: ID of the Deduction
          required: true
      - name: patchhrisdeduction
        method: PATCH
        description: Update a deduction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: id
          in: path
          type: string
          description: ID of the Deduction
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: updatehrisdeduction
        method: PUT
        description: Update a deduction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: fields
          in: query
          type: array
          description: Fields to return
        - name: raw
          in: query
          type: string
          description: 'Raw parameters to include in the 3rd-party request. Encoded as a URL component. eg. raw parameters:
            foo=bar&zoo=bar -> raw=foo%3Dbar%26zoo%3Dbar'
        - name: connection_id
          in: path
          type: string
          description: ID of the connection
          required: true
        - name: id
          in: path
          type: string
          description: ID of the Deduction
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: authorization
      value: '{{env.UNIFIED_TO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: hris-deduction-rest
    port: 8080
    description: REST adapter for Unified.to  API — deduction. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/hris/{connection-id}/deduction
      name: hris-connection-id-deduction
      description: REST surface for hris-connection_id-deduction.
      operations:
      - method: GET
        name: listhrisdeductions
        description: List all deductions
        call: hris-deduction.listhrisdeductions
        with:
          limit: rest.limit
          offset: rest.offset
          updated_gte: rest.updated_gte
          sort: rest.sort
          order: rest.order
          query: rest.query
          user_id: rest.user_id
          company_id: rest.company_id
          payslip_id: rest.payslip_id
          benefit_id: rest.benefit_id
          start_gte: rest.start_gte
          end_lt: rest.end_lt
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createhrisdeduction
        description: Create a deduction
        call: hris-deduction.createhrisdeduction
        with:
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/hris/{connection-id}/deduction/{id}
      name: hris-connection-id-deduction-id
      description: REST surface for hris-connection_id-deduction-id.
      operations:
      - method: DELETE
        name: removehrisdeduction
        description: Remove a deduction
        call: hris-deduction.removehrisdeduction
        with:
          connection_id: rest.connection_id
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: gethrisdeduction
        description: Retrieve a deduction
        call: hris-deduction.gethrisdeduction
        with:
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
          id: rest.id
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patchhrisdeduction
        description: Update a deduction
        call: hris-deduction.patchhrisdeduction
        with:
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
          id: rest.id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatehrisdeduction
        description: Update a deduction
        call: hris-deduction.updatehrisdeduction
        with:
          fields: rest.fields
          raw: rest.raw
          connection_id: rest.connection_id
          id: rest.id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: hris-deduction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Unified.to  API — deduction. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-all-deductions
      description: List all deductions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: hris-deduction.listhrisdeductions
      with:
        limit: tools.limit
        offset: tools.offset
        updated_gte: tools.updated_gte
        sort: tools.sort
        order: tools.order
        query: tools.query
        user_id: tools.user_id
        company_id: tools.company_id
        payslip_id: tools.payslip_id
        benefit_id: tools.benefit_id
        start_gte: tools.start_gte
        end_lt: tools.end_lt
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
      outputParameters:
      - type: object
        mapping: $.
    - name: create-deduction
      description: Create a deduction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: hris-deduction.createhrisdeduction
      with:
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: remove-deduction
      description: Remove a deduction
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: hris-deduction.removehrisdeduction
      with:
        connection_id: tools.connection_id
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieve-deduction
      description: Retrieve a deduction
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: hris-deduction.gethrisdeduction
      with:
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: update-deduction
      description: Update a deduction
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: hris-deduction.patchhrisdeduction
      with:
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
        id: tools.id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: update-deduction-2
      description: Update a deduction
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: hris-deduction.updatehrisdeduction
      with:
        fields: tools.fields
        raw: tools.raw
        connection_id: tools.connection_id
        id: tools.id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
